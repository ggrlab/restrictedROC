# Prune a rROC result

Prune a rROC result

## Usage

``` r
# S3 method for class 'rROC'
prune(x, ...)
```

## Arguments

- x:

  rROC object returned by
  [`rROC`](https://ggrlab.github.io/restrictedROC/reference/rROC.md).
